# ==========================================
# Experiment Control Configuration
# ==========================================
experiment:
  # What to scale? (Options: "noise", "clutter", "drops", "all")
  target: "drops" 
  
  # Sweep Parameters
  start_multiplier: 1
  max_multiplier: 5
  step_size: 0.5